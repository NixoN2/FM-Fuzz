name: Build Bitwuzla

on:
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        
      - name: Build Bitwuzla
        run: ./scripts/bitwuzla/build.sh
